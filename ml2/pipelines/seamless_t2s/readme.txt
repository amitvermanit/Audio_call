to set up environment:

- create new conda env with python 3.10: conda create -n T2S_ENV python==3.10
- enter environment with conda activate T2S_ENV
- run conda install -c conda-forge libsndfile==1.0.31
- run pip install -r requirements.txt
